# Auto-detect text files and perform LF normalization
* text=auto eol=lf

# Explicitly declare text files we care about to ensure LF endings in the repo
*.js         text eol=lf
*.json       text eol=lf
*.css        text eol=lf
*.html       text eol=lf
Dockerfile   text eol=lf
*.toml       text eol=lf
*.md         text eol=lf
*.gitignore  text eol=lf
*.gitattributes text eol=lf

# Declare files that should always have CRLF (if any, usually none needed for web dev)
# *.bat      text eol=crlf

# Declare files that should not be touched (binary files)
# *.png      binary
# *.jpg      binary